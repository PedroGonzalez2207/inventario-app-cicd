apiVersion: v1
kind: Service
metadata:
  name: inventario-app-canary-service
spec:
  type: NodePort
  selector:
    app: inventario-app-canary
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: http
